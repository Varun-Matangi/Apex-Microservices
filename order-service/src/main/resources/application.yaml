server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: ORDER-SERVICE

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info

info:
  application:
    name: ORDER-SERVICE
    version: 1.0.0